<?php
    $a = 6;
    $b = 2;

    // Menggunakan operator relasi lebih
    $c = $a > $b;
    echo "$a > $b: $c";
    echo "<hr>";

    // Menggunakan operator relasi kecil
    $c = $a < $b;
    echo "$a < $b: $c";
    echo "<hr>";

    // Menggunakan operator relasi leboh dan sama dengan
    $c = $a == $b;
    echo "$a == $b: $c";
    echo "<hr>";

    // Menggunakan operator relasi lebih tidak sama dengan
    $c = $a != $b;
    echo "$a != $b: $c";
    echo "<hr>";

    // Menggunakan operator relasi lebih besar dengan
    $c = $a >= $b;
    echo "$a >= $b: $c";
    echo "<hr>";

    // MEnggunakan operator relasi lebih kecil sama dengan
    $c = $a <= $b;
    echo "$a <= $b: $c";
    echo "<hr>";

?>